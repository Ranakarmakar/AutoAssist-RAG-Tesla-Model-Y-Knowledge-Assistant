# Production Environment Configuration
# Copy this file to .env and adjust values as needed

# Required: Groq API Key
GROQ_API_KEY=your_production_groq_api_key_here

# LLM Configuration
LLM_MODEL=llama-3.3-70b-versatile
LLM_TEMPERATURE=0.1

# Embedding Model
EMBEDDING_MODEL=sentence-transformers/all-mpnet-base-v2

# Reranking Model
RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2

# Chunking Configuration - Optimized for production
CHUNK_SIZE=1200
CHUNK_OVERLAP=150

# Retrieval Configuration - Balanced for performance
RETRIEVAL_TOP_K=15
RERANK_TOP_K=5
BM25_WEIGHT=0.3
SEMANTIC_WEIGHT=0.7

# Storage Paths - Use persistent volumes in production
VECTOR_STORE_PATH=/app/data/vector_store
UPLOAD_PATH=/app/data/uploads

# Cache Configuration - Larger caches for production
ENABLE_CACHING=true
CACHE_MEMORY_SIZE=5000
ENABLE_FILE_CACHE=true
CACHE_DIR=/app/data/cache
EMBEDDING_CACHE_TTL=2592000  # 30 days in seconds
QUERY_CACHE_TTL=7200         # 2 hours in seconds

# Redis Cache (optional for distributed caching)
# REDIS_URL=redis://redis-server:6379/0

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Logging - Reduced verbosity for production
LOG_LEVEL=INFO

# Production-specific settings
# - Optimized cache sizes and TTLs
# - Persistent storage paths
# - Reduced logging verbosity
# - Optional Redis for distributed caching
# - Longer cache TTLs for better performance